# AI knowledge gate

This marker intentionally triggers the repository CI gate after the Ada intelligence upgrade. The workflow remains the source of truth for image smoke tests and GHCR publication.
